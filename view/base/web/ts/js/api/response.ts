export declare type ConfigWorkerContent = {
    baseUrl: string,
    assetUrl: {
        sessionLoader: string,
        requireWorker: string,
        requirejs: string,
        requireMixins: string,
        requireConfig: string,
    }
}
